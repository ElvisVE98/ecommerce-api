import Navbar from "./Navbar";

const CartPage = () =>{
    return(
        <div>
            <Navbar />
            <main>
                <h1>carrito</h1>
            </main>
        </div>

    );
};

export default CartPage;